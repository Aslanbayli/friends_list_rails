<div class="card">
  <div class="card-header">
    <h1>Make Changes to Friend</h1>
  </div>
  <div class="card-body">
    <%= render 'form', friend: @friend%>
  </div>
</div>

<br/>

<%= link_to 'Show', @friend, class:"btn btn-secondary" %> &nbsp
<%= link_to 'Delete', @friend, method: :delete, data: { confirm: 'Are you sure?' }, class:'link1', class:"btn btn-danger" %>&nbsp
<%= link_to 'Back', friends_path, class:"btn btn-dark" %>       
